# Telegram Elevator Invoice Bot

ربات تلگرام برای صدور خودکار پیش‌فاکتور آسانسور با قابلیت محاسبات پیشرفته و تولید PDF فارسی

## ویژگی‌ها

- 🤖 رابط کاربری تعاملی با Telegram Bot
- 💰 محاسبه خودکار قیمت بر اساس مشخصات آسانسور
- 📊 مدیریت ضرایب قیمت‌گذاری (توسط ادمین)
- 📄 تولید خودکار PDF پیش‌فاکتور به زبان فارسی
- 💾 ذخیره‌سازی اطلاعات در پایگاه داده SQLite
- 🔐 مدیریت دسترسی کاربران (ادمین/کاربر عادی)

## نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.8 یا بالاتر
- pip (مدیریت پکیج‌های Python)

### مراحل نصب

1. کلون کردن پروژه:
```bash
git clone <repository-url>
cd TelBot
```

2. نصب پکیج‌های مورد نیاز:
```bash
pip install -r requirements.txt
```

3. تنظیم فایل کانفیگ:
```python
# config.py
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
ADMIN_USER_ID = YOUR_TELEGRAM_USER_ID
```

4. اجرای اسکریپت seed برای ایجاد داده‌های اولیه:
```bash
python seed_data.py
```

5. اجرای ربات:
```bash
python main.py
```

## ساختار پروژه

```
TelBot/
├── config.py              # تنظیمات و توکن ربات
├── main.py               # فایل اصلی اجرای ربات
├── requirements.txt      # وابستگی‌های پروژه
├── seed_data.py         # داده‌های اولیه پایگاه داده
├── bot/
│   ├── __init__.py
│   ├── handlers.py      # هندلرهای پیام‌های کاربر
│   └── admin.py         # هندلرهای ادمین
├── db/
│   ├── __init__.py
│   └── database.py      # مدیریت پایگاه داده
├── logic/
│   ├── __init__.py
│   └── calculator.py    # محاسبات قیمت‌گذاری
├── pdf/
│   ├── __init__.py
│   └── generator.py     # تولید PDF
├── templates/
│   └── invoice_template.html  # قالب HTML پیش‌فاکتور
└── output/              # فولدر خروجی PDF‌ها
```

## استفاده

### کاربران عادی

1. شروع ربات: `/start`
2. انتخاب نوع آسانسور (هیدرولیک/جکی)
3. وارد کردن مشخصات:
   - تعداد طبقات
   - تعداد ایستگاه‌ها
   - نوع درب (اتوماتیک/دستی)
   - کابین استاندارد یا لوکس
4. دریافت پیش‌فاکتور PDF

### ادمین

- مشاهده ضرایب فعلی: `/admin`
- تغییر ضرایب قیمت‌گذاری
- مدیریت قیمت‌های پایه

## تکنولوژی‌های استفاده شده

- **python-telegram-bot**: رابط ربات تلگرام
- **SQLite**: پایگاه داده
- **WeasyPrint**: تولید PDF از HTML
- **Jinja2**: موتور قالب‌سازی

## مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## توسعه‌دهندگان

برای مشارکت در پروژه، لطفاً یک Pull Request ارسال کنید.
