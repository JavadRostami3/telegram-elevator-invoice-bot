# Telegram Elevator Invoice Bot

ربات تلگرام برای صدور خودکار پیش‌فاکتور آسانسور با قابلیت محاسبات پیشرفته و تولید PDF فارسی.

## ویژگی‌ها

- 🤖 رابط کاربری تعاملی با Telegram Bot
- 💰 محاسبه خودکار قیمت بر اساس مشخصات آسانسور
- 📊 مدیریت ضرایب قیمت‌گذاری (توسط ادمین)
- 📄 تولید PDF پیش‌فاکتور به زبان فارسی
- 💾 ذخیره‌سازی اطلاعات در پایگاه داده SQLite
- 🔐 مدیریت دسترسی کاربران (ادمین/کاربر عادی)

## نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.8+
- wkhtmltopdf (برای تولید PDF با `pdfkit`)

### مراحل نصب

```bash
git clone https://github.com/JavadRostami3/telegram-elevator-invoice-bot.git
cd telegram-elevator-invoice-bot

python -m venv .venv
source .venv/bin/activate

pip install -r requirements.txt
```

### تنظیمات

```bash
cp .env.example .env
# سپس مقادیر زیر را در .env تنظیم کنید
# BOT_TOKEN=...
# ADMIN_CHAT_IDS=...
```

### اجرا

```bash
python main.py
```